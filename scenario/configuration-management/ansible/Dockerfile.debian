FROM debian:stable-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends ansible \
  && which ansible-playbook

COPY scenario/configuration-management/ansible/playbook.yml /playbook.yml

CMD \
  ansible-playbook /playbook.yml; \
  exec apache2ctl -v;
